const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const sections={recruiter:['experience','projects','skills'],evidence:['evidence','judge','research','awards'],research:['research','projects','opensource'],explorer:['projects','opensource','skills']};
function setMode(mode){$$('.mode').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));$$('[data-audience]').forEach(el=>{const a=el.dataset.audience.split(' ');el.classList.toggle('hidden',mode!=='all'&&!a.includes(mode))});localStorage.setItem('portfolioMode',mode)}
$$('.mode').forEach(b=>b.onclick=()=>setMode(b.dataset.mode));setMode(localStorage.getItem('portfolioMode')||'all');
$$('.expand').forEach(b=>b.onclick=()=>{const d=b.closest('.card').querySelector('.details');d.classList.toggle('open');b.textContent=d.classList.contains('open')?'Hide role details ↑':'Explore full role ↓'});
const drawer=$('#drawer'), drawerBody=$('#drawerBody');function closeDrawer(){drawer.classList.remove('open')}$('#drawerClose').onclick=closeDrawer;$('.shade',drawer).onclick=closeDrawer;
$$('.project').forEach(p=>p.onclick=e=>{if(e.target.closest('a'))return;drawerBody.innerHTML=`<p class="eye">${p.dataset.type||'PROJECT CASE STUDY'}</p><h2>${p.dataset.title||$('h4',p).textContent}</h2><p>${p.dataset.summary||$('p:not(.eye)',p)?.textContent||''}</p><h3>Why this matters</h3><p>${p.dataset.why||'This work connects applied engineering with research-oriented problem solving and a reproducible technical artifact.'}</p><h3>Technical focus</h3><p>${p.dataset.stack||'AI/ML · software engineering · APIs · data systems'}</p><h3>Evidence</h3><p>Use the linked repository, publication, deployment, or program evidence to inspect the underlying work.</p>`;drawer.classList.add('open')});
const cmd=$('#cmd'), q=$('#cmdInput');function openCmd(){cmd.classList.add('open');q.focus();renderCmd('')}function closeCmd(){cmd.classList.remove('open')}$('#command').onclick=openCmd;cmd.onclick=e=>{if(e.target===cmd)closeCmd()};document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openCmd()}if(e.key==='Escape'){closeCmd();closeDrawer()}});
const commands=[['Work experience','#experience'],['O-1A / EB-1A evidence','#evidence'],['Technovation Judge Gold','#judge'],['Research publications','#research'],['Hackathon projects','#projects'],['GSSoC & SuperBrowser','#opensource'],['Technical symposium wins','#awards'],['AI certifications','#certifications'],['AI / engineering skills','#skills']];function renderCmd(v){$('#cmdResults').innerHTML=commands.filter(x=>x[0].toLowerCase().includes(v.toLowerCase())).map(x=>`<button data-go="${x[1]}">${x[0]}</button>`).join('');$$('[data-go]',$('#cmdResults')).forEach(b=>b.onclick=()=>{closeCmd();$(b.dataset.go)?.scrollIntoView({behavior:'smooth'})})}q.oninput=()=>renderCmd(q.value);renderCmd('');
const search=$('#portfolioSearch');if(search)search.oninput=()=>{const v=search.value.toLowerCase();$$('[data-search]').forEach(el=>el.classList.toggle('hidden',!el.dataset.search.toLowerCase().includes(v)))};
$$('.filter').forEach(f=>f.onclick=()=>{$$('.filter').forEach(x=>x.classList.remove('active'));f.classList.add('active');const k=f.dataset.filter;$$('[data-category]').forEach(el=>el.classList.toggle('hidden',k!=='all'&&!el.dataset.category.includes(k)))});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.animate([{opacity:.25,transform:'translateY(14px)'},{opacity:1,transform:'none'}],{duration:500,fill:'both'})}),{threshold:.08});$$('.section').forEach(x=>io.observe(x));